# sixdof/ — intentionally empty

Reserved for native 6-DOF pose helpers. Head tracking currently comes from
Android's game rotation vector (fused on the phone, forwarded as tag 0x12),
so no native 6-DOF code exists yet. New native pose code belongs here.
