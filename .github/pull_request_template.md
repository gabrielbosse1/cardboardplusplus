## What changed

## How I tested

- [ ] `cargo test --manifest-path bridge/Cargo.toml` (bridge)
- [ ] Driver build (`scripts/compile-driver.ps1` or CI)
- [ ] Android build (`gradlew testDebugUnitTest` / `assembleDebug` or CI)
- [ ] Real-world test (needs SteamVR PC + Android phone — if you can't, say so; the maintainer covers hardware steps)

## Notes for the reviewer
