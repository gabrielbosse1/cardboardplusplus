[package]
name = "bridge-core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Core bridge logic: consumes the shared ring, portable default paths."

[dependencies]
bridge-shm = { path = "../bridge-shm" }
serde_json = "1"